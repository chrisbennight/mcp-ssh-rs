{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "lockFileMaintenance": {
    "enabled": false
  },
  "osvVulnerabilityAlerts": true,
  "vulnerabilityAlerts": {
    "enabled": true
  },
  "enabledManagers": ["cargo", "dockerfile", "github-actions", "custom.regex"],
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "Cargo updates are security-only; image and workflow references remain eligible for routine updates.",
      "matchManagers": ["cargo"],
      "enabled": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update explicitly pinned Rust tool versions.",
      "managerFilePatterns": ["/^rust-toolchain\\.toml$/", "/^\\.github/workflows/ci\\.yml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>[^\\s]+)\\s*\\n\\s*(?:[A-Z0-9_]+|channel)\\s*[:=]\\s*\"(?<currentValue>[^\"]+)\""
      ]
    }
  ]
}
