# Project Instructions

All conventions, constraints, and build commands live in `AGENTS.md` (the
single source of truth for repo guidance). `CLAUDE.md` exists because some
tools load it by convention; all content is in `AGENTS.md` to avoid
duplication.

See @AGENTS.md
